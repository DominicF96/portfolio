export interface NavbarLink {
  key: string;
  url: string;
}

const navbarLinks = [
  { key: "home", url: "/" },
  // { key: "case_study", url: "/" },
  // { key: "dashboard_template", url: "/" },
  { key: "blog", url: "/blog" },
];

export default navbarLinks;
